using UnityEngine;
using UnityEditor;

namespace Bakhanov.IPD
{
    public enum PointStatus
    {
        ACTIVE,
        FIXED
    }

    public enum EdgeStatus
    {
        ACTIVE,
        INNER,
        BOUNDARY
    }
}